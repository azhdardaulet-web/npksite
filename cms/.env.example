# ─── Database ─────────────────────────────────────────────────────────────────
# В Docker: хост = postgres (имя сервиса compose). Вне Docker: localhost
DATABASE_URL="postgresql://darrail:CHANGE_ME@postgres:5432/darrail_cms"
POSTGRES_DB=darrail_cms
POSTGRES_USER=darrail
POSTGRES_PASSWORD=CHANGE_ME

# ─── JWT ──────────────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_64_CHAR_HEX_STRING
JWT_REFRESH_SECRET=CHANGE_ME_DIFFERENT_64_CHAR_HEX_STRING
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ─── MinIO ────────────────────────────────────────────────────────────────────
# В Docker: minio. Вне Docker: localhost
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minio_admin
MINIO_SECRET_KEY=CHANGE_ME_MINIO_PASSWORD
MINIO_BUCKET=darrail-media
# Public URL where MinIO files are accessible
MINIO_PUBLIC_URL=https://darrail.kz/media

# ─── hCaptcha ─────────────────────────────────────────────────────────────────
HCAPTCHA_SECRET=CHANGE_ME_HCAPTCHA_SECRET_KEY

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:5173,http://localhost:8080,https://darrail.kz,https://www.darrail.kz,https://cms.darrail.kz

# ─── Email (for form submission notifications) ────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@darrail.com
SMTP_PASS=CHANGE_ME_APP_PASSWORD
SMTP_FROM="DAR Rail CMS <noreply@darrail.com>"
NOTIFICATION_EMAIL=procurement@darrail.com

# ─── CMS Frontend (Vite) ──────────────────────────────────────────────────────
VITE_API_URL=http://localhost:3001
VITE_HCAPTCHA_SITE_KEY=CHANGE_ME_HCAPTCHA_SITE_KEY
